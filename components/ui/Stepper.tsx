
import React from 'react';

// This component is no longer used in the new single-page booking layout.
// It is kept to avoid breaking potential future references but can be safely deleted.
const Stepper: React.FC = () => {
  return null;
};

export default Stepper;
